# Copy to .env (cp env.example .env) and fill in. .env is gitignored — never commit real values.

# OpenAI-compatible gateway for Claude on AWS Bedrock
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Hugging Face (optional, dataset pulls)
HF_TOKEN=

# App
HOSPITAL_PROFILE=urban_500
